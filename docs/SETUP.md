# Configuração do Ambiente

## Pré-requisitos
- CMake 3.15+
- Compilador C++20
- Git

## Desenvolvimento
1. Clone o repositório
2. Execute `cmake -B build`
3. Compile com `cmake --build build`
4. Execute `./build/CitySimulator`