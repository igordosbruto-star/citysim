#include <gtest/gtest.h>

// Placeholder test suite for the ECS module defined in PLAN_M01 Phase 3.
// The suite is intentionally skipped until the ECS systems are implemented.
TEST(EcsRoadmap, DISABLED_Placeholder) {
    GTEST_SKIP() << "ECS tests will be added once the ECS module is implemented.";
}
